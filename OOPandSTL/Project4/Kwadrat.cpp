#include "Kwadrat.h"
#include "Prostokat.h"

using namespace lab4;

/*
bool Kwadrat::zapisz(ostream & os)const
{
    bool c = Figura::zapisz(os);
    os << ", boki: " << a << ", pole: " << pole();
    return c && os;
}

bool Kwadrat::wczytaj(istream & is)
{
    bool c = Figura::wczytaj(is);
    is >> a >> b;
    return c && is;
}
*/
